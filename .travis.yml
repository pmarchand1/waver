language: r
warnings_are_errors: true
sudo: required

apt-packages:
   - libgdal-dev
   - libproj-dev

env:
 global:
   - CRAN: http://cran.rstudio.com

notifications:
  email:
    on_success: change
    on_failure: change
